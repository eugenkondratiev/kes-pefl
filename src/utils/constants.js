const NETLIFY_FUNCTIONS = "/.netlify/functions/";

    const cupIds_REF = NETLIFY_FUNCTIONS + 'cups/cupids';
    const intercupIds_REF = NETLIFY_FUNCTIONS + 'cups/ecs';
    const extracupIds_REF = NETLIFY_FUNCTIONS + 'cups/extras';  
    const cupById_REF = NETLIFY_FUNCTIONS + 'cups/cup';  

module.exports = {
    NETLIFY_FUNCTIONS ,
    cupIds_REF,
    intercupIds_REF,
    extracupIds_REF,
    cupById_REF
 
}